### 1. 稳定排序与不稳定排序

稳定排序：冒泡，插入，归并都是稳定的

不稳定：所有其他的，选择（注意选择排序是靠交换完成的）

### 2. 布隆过滤器

布隆过滤器

应用：海量无序数据的查找

哈希+位图：是概率性存在的查找方式

本质：bit向量数组

对于每个输入，使用多个哈希函数生成值，值生成的hash值bit为1

查询：若某位为0，则必不存在，而若全为1，则概率存在

删除：位图形式不支持，但是可以为位图增加计数，增加时加1，删除时减1

元素越来越大，hash冲突的可能性越高